const insert = document.getElementById('insert')

window.addEventListener('keydown', (e) => {
    console.log(e)
})


// FROM THE CONSOLE (Target Values)
// code - code
// key - key
// keyCode - keyCode